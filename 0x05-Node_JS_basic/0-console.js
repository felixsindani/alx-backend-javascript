/**
 * prints in STDOUT the string argument
 * @param {Strin} arg The str argument to display
 * @returns
 */
const displayMessage = (arg) => {
  console.log(arg);
};
module.exports = displayMessage;
