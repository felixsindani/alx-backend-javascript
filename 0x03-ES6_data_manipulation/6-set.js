/**
 * function that returns a Set from an array.
 * @param {[*]} arr - Accepts Array, of any kind of element)
 * @returns a set
 */
export default function setFromArray(arr) {
  return new Set(arr);
}
